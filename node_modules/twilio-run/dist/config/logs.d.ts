import { ClientConfig, LogsConfig as ApiLogsConfig } from '@twilio-labs/serverless-api';
import { Arguments } from 'yargs';
import { ExternalCliOptions, SharedFlagsWithCredentials } from '../commands/shared';
export declare type LogsConfig = ClientConfig & ApiLogsConfig & {
    username: string;
    password: string;
    cwd: string;
    properties?: string[];
    outputFormat?: string;
};
export declare type LogsCliFlags = Arguments<SharedFlagsWithCredentials & {
    cwd?: string;
    environment?: string;
    serviceSid?: string;
    functionSid?: string;
    tail: boolean;
    outputFormat?: string;
}>;
export declare function getConfigFromFlags(flags: LogsCliFlags, externalCliOptions?: ExternalCliOptions): Promise<LogsConfig>;
